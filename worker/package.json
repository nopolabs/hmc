{
	"name": "worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"secrets:dev": "node set-secrets.js dev",
		"secrets:prod": "node set-secrets.js prod",
		"deploy:dev": "node set-secrets.js dev && wrangler deploy",
		"deploy:prod": "node set-secrets.js prod && wrangler deploy"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"vitest": "~3.2.0",
		"wrangler": "^4.69.0"
	},
	"dependencies": {
		"stripe": "^20.4.0"
	}
}
