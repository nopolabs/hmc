{
	"name": "worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"deploy:dev": "wrangler deploy --env dev",
		"deploy:prod": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"secrets:dev": "node set-secrets.js dev",
		"secrets:prod": "node set-secrets.js prod"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"vitest": "~3.2.0",
		"wrangler": "^4.69.0"
	},
	"dependencies": {
		"stripe": "^20.4.0"
	}
}
